<%= form_for(micropost.likes.find_by(micropost_id: micropost.id),
             html: {method: :delete},
             remote: true) do |f| %>
    <%= f.hidden_field :micropost_id, :value => micropost.id %>
    <%= f.submit "Unlike",id: micropost.id, onclick: "refresh_div(#{micropost.id},#{current_user.id})", class: "btn btn-small" %>

<% end %>